# Frozen Character Classifier

**Deep learning with small data**

**Try it live on Hugging Face Spaces:** [classify an image](https://huggingface.co/spaces/quidditch/deep-learning-image-classifier) or play the [kids' edition](https://huggingface.co/spaces/quidditch/frozen-ai-kids) ("how does the computer know?").

Built a convolutional neural network (CNN) image classifier during Jeremy Howard's Practical Deep Learning for Coders (fast.ai). The model tells apart three characters from Disney's *Frozen*: Olaf the snowman, Elsa, and Sven the reindeer.

Trained on about 34 images per class (101 total) using transfer learning, it classifies the small held-out validation set (20 images) with 100% accuracy, an encouraging result on limited data rather than a large-scale benchmark. The goal: show you don't need a large dataset to build an accurate classifier, and make AI tangible and fun for kids.

No children were harmed in training or testing this CNN, but we may have tricked them into learning a bit about AI. ;)

**Technologies:** Python, fast.ai (built on PyTorch)  
**Technique:** Transfer learning  
**Base model:** ResNet-18, pre-trained on ImageNet  
**Dataset:** ~34 curated, single-character images per class, sourced from curated URL lists  
**Result:** 100% accuracy on the held-out validation set (81 train / 20 validation)

## Approach

ResNet-18 is one of the smallest CNN architectures, which suits a small dataset. Traditionally this kind of image classification meant collecting thousands of labeled examples. Transfer learning changes that: start with a model that already understands visual features, freeze the early layers, train the new head for one epoch, then unfreeze and fine-tune all layers.

The three characters were chosen to separate on both color and shape: Olaf (white snowman), Sven (brown reindeer), and Elsa (a human in blue). Training images are curated to show one character alone, so the model learns the subject rather than a co-star or a background.

## Limitations and path to production

It recognizes these three characters only. The live demo applies a confidence threshold, so unclear or out-of-distribution images get an honest "not sure" instead of a forced guess. Further production hardening would add a dedicated reject/unknown class, a larger labeled test set (the 20-image validation set proves the approach, not a precise real-world number), and drift monitoring on served inputs. The training images are web-sourced character art, so a commercial deployment would need licensed or synthetic data.

## Repo contents

- `frozen_train.ipynb`: data download, model training, interpretation, and export
- `data/`: curated URL lists, one per class (`urls_olaf.csv`, `urls_elsa.csv`, `urls_sven.csv`)
- `export.pkl`: exported model (generated by the training notebook)
- `hf-space/`: Gradio app + config for the live Hugging Face Space demo
- `hf-kid-space/`: pick-guess-reveal teaching demo (kids' edition)
- `gallery_curation.ipynb`: how the kids' gallery and answer labels were derived (Grad-CAM driven)

## License

Code is released under the [MIT License](LICENSE). This covers the notebooks and app code only. The Frozen character images in `*/examples/` and the trained `export.pkl` derive from Disney's *Frozen* and are included for non-commercial, educational use; they are not relicensed under MIT.
